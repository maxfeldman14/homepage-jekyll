---
layout: default 
type: projects
---
%ul.posts
  {% for post in site.categories.blog %}
  %li
    %span {{post.date | date_to_string }} &raquo;
    %a{:href => "{{ post.url }}"} {{post.title}}
    %p {{post.description}}
  {% endfor %}
